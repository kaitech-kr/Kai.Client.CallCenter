# 주문 분류 로직 확정안

**작성일**: 2025-10-23
**작성자**: Claude Code
**목적**: 4개 외부앱(인성1, 인성2, 화물24시, 원콜)으로 주문을 분류하는 절대 기준 정의

---

## 📋 목차
1. [TbOrder 모델 구조](#1-tborder-모델-구조)
2. [분류 기준 (절대 규칙)](#2-분류-기준-절대-규칙)
3. [분류 Decision Tree](#3-분류-decision-tree)
4. [구체적인 예시](#4-구체적인-예시)
5. [구현 코드](#5-구현-코드)

---

## 1. TbOrder 모델 구조

**핵심 필드** (Kai.Server.Main/KaiWork/DBs/Postgres/KaiDB/Models/TbOrder.cs):

```csharp
public class TbOrder
{
    public long KeyCode { get; set; }                    // 주문 키

    // 차량 정보
    public string CarType { get; set; } = null!;         // ⭐ 차량 종류 (오토, 트럭 등)
    public string CarWeight { get; set; } = null!;       // ⭐ 차량 무게 (1t, 1.4t, 2.5t 등)

    // 고객 정보
    public string CallCustFrom { get; set; } = null!;    // ⭐ 의뢰자 (인성1, 인성2 등)
    public string FeeType { get; set; } = null!;         // ⭐ 요금 타입 (신용, 현금 등)

    // 외부앱 SeqNo (배차 완료 후 저장)
    public string Insung1 { get; set; } = null!;
    public string Insung2 { get; set; } = null!;
    public string Cargo24 { get; set; } = null!;
    public string Onecall { get; set; } = null!;
}
```

---

## 2. 분류 기준 (절대 규칙)

### 2.1 차량 타입 판단

**절대적 기준**:
1. `CarType == "오토"` → **오토바이**
2. `CarType == "트럭"` 중:
   - `CarWeight == "1t"` 또는 `"1.4t"` → **1.4톤 이하 트럭**
   - 그 외 (`"2.5t"`, `"5t"` 등) → **1.4톤 초과 트럭**

### 2.2 외부앱별 분배 규칙

| 차량 타입 | 인성1 | 인성2 | 화물24시 | 원콜 |
|-----------|-------|-------|----------|------|
| **오토바이** | ✅ (신용업체 필터링) | ✅ (신용업체 필터링) | ❌ | ❌ |
| **1.4톤 이하 트럭** | ✅ (신용업체 필터링) | ✅ (신용업체 필터링) | ✅ | ✅ |
| **1.4톤 초과 트럭** | ❌ | ❌ | ✅ | ✅ |

### 2.3 신용업체 필터링 (인성1, 인성2만 적용)

- **인성1 추가 조건**: `!(CallCustFrom == "인성2" && FeeType == "신용")`
  - 의미: **인성2 신용업체**의 주문은 **인성1에서 제외** (인성2로만 보냄)

- **인성2 추가 조건**: `!(CallCustFrom == "인성1" && FeeType == "신용")`
  - 의미: **인성1 신용업체**의 주문은 **인성2에서 제외** (인성1로만 보냄)

- **화물24시, 원콜**: 신용업체 필터링 없음 (모두 추가)

---

## 3. 분류 Decision Tree

```
주문 받음 (TbOrder)
  ↓
┌──────────────────────────────────────────────────────┐
│ Step 1: 차량 타입 판단                                │
└──────────────────────────────────────────────────────┘
  ├─ CarType == "오토"
  │   → 오토바이
  │
  └─ CarType == "트럭"
      ├─ CarWeight == "1t" 또는 "1.4t"
      │   → 1.4톤 이하 트럭
      │
      └─ 그 외 (2.5t, 5t, 11t 등)
          → 1.4톤 초과 트럭

┌──────────────────────────────────────────────────────┐
│ Step 2: 외부앱별 분배                                 │
└──────────────────────────────────────────────────────┘

[오토바이] (CarType == "오토")
  ├─ 인성1 체크
  │   조건: !(CallCustFrom=="인성2" && FeeType=="신용")
  │   ✅ 참 → 인성1 큐 추가
  │   ❌ 거짓 → 인성1 제외
  │
  ├─ 인성2 체크
  │   조건: !(CallCustFrom=="인성1" && FeeType=="신용")
  │   ✅ 참 → 인성2 큐 추가
  │   ❌ 거짓 → 인성2 제외
  │
  ├─ 화물24시: ❌ 제외
  └─ 원콜: ❌ 제외

[1.4톤 이하 트럭] (CarType == "트럭" && (CarWeight == "1t" || "1.4t"))
  ├─ 인성1 체크
  │   조건: !(CallCustFrom=="인성2" && FeeType=="신용")
  │   ✅ 참 → 인성1 큐 추가
  │
  ├─ 인성2 체크
  │   조건: !(CallCustFrom=="인성1" && FeeType=="신용")
  │   ✅ 참 → 인성2 큐 추가
  │
  ├─ 화물24시: ✅ 무조건 추가
  └─ 원콜: ✅ 무조건 추가

[1.4톤 초과 트럭] (CarType == "트럭" && CarWeight != "1t", "1.4t")
  ├─ 인성1: ❌ 제외
  ├─ 인성2: ❌ 제외
  ├─ 화물24시: ✅ 무조건 추가
  └─ 원콜: ✅ 무조건 추가
```

---

## 4. 구체적인 예시

### 예시 1: 오토바이 (일반 고객)
```
주문 정보:
- CarType: "오토"
- CarWeight: "" (무관)
- CallCustFrom: "직접접수"
- FeeType: "현금"

분류 결과:
✅ 인성1 큐 추가 (!(직접접수=="인성2" && 현금=="신용") = true)
✅ 인성2 큐 추가 (!(직접접수=="인성1" && 현금=="신용") = true)
❌ 화물24시 제외
❌ 원콜 제외

→ 총 2개 큐: 인성1, 인성2
```

### 예시 2: 오토바이 (인성2 신용업체)
```
주문 정보:
- CarType: "오토"
- CallCustFrom: "인성2"
- FeeType: "신용"

분류 결과:
❌ 인성1 큐 제외 (!(인성2=="인성2" && 신용=="신용") = false)
✅ 인성2 큐 추가 (!(인성2=="인성1" && 신용=="신용") = true)
❌ 화물24시 제외
❌ 원콜 제외

→ 총 1개 큐: 인성2만
```

### 예시 3: 1톤 트럭 (일반 고객)
```
주문 정보:
- CarType: "트럭"
- CarWeight: "1t"
- CallCustFrom: "직접접수"
- FeeType: "현금"

분류 결과:
✅ 인성1 큐 추가
✅ 인성2 큐 추가
✅ 화물24시 큐 추가
✅ 원콜 큐 추가

→ 총 4개 큐: 인성1, 인성2, 화물24시, 원콜 (모두!)
```

### 예시 4: 1.4톤 트럭 (인성1 신용업체)
```
주문 정보:
- CarType: "트럭"
- CarWeight: "1.4t"
- CallCustFrom: "인성1"
- FeeType: "신용"

분류 결과:
✅ 인성1 큐 추가 (!(인성1=="인성2" && 신용=="신용") = true)
❌ 인성2 큐 제외 (!(인성1=="인성1" && 신용=="신용") = false)
✅ 화물24시 큐 추가
✅ 원콜 큐 추가

→ 총 3개 큐: 인성1, 화물24시, 원콜
```

### 예시 5: 2.5톤 트럭
```
주문 정보:
- CarType: "트럭"
- CarWeight: "2.5t"
- CallCustFrom: "직접접수"
- FeeType: "현금"

분류 결과:
❌ 인성1 큐 제외 (1.4톤 초과)
❌ 인성2 큐 제외 (1.4톤 초과)
✅ 화물24시 큐 추가
✅ 원콜 큐 추가

→ 총 2개 큐: 화물24시, 원콜
```

### 예시 6: 5톤 트럭 (신용업체)
```
주문 정보:
- CarType: "트럭"
- CarWeight: "5t"
- CallCustFrom: "인성2"
- FeeType: "신용"

분류 결과:
❌ 인성1 큐 제외 (1.4톤 초과)
❌ 인성2 큐 제외 (1.4톤 초과)
✅ 화물24시 큐 추가 (신용업체 필터링 없음)
✅ 원콜 큐 추가 (신용업체 필터링 없음)

→ 총 2개 큐: 화물24시, 원콜
```

### 요약표

| 케이스 | CarType | CarWeight | CallCustFrom | FeeType | 인성1 | 인성2 | 화물24시 | 원콜 |
|--------|---------|-----------|--------------|---------|-------|-------|----------|------|
| 1 | 오토 | - | 직접접수 | 현금 | ✅ | ✅ | ❌ | ❌ |
| 2 | 오토 | - | 인성2 | 신용 | ❌ | ✅ | ❌ | ❌ |
| 3 | 트럭 | 1t | 직접접수 | 현금 | ✅ | ✅ | ✅ | ✅ |
| 4 | 트럭 | 1.4t | 인성1 | 신용 | ✅ | ❌ | ✅ | ✅ |
| 5 | 트럭 | 2.5t | 직접접수 | 현금 | ❌ | ❌ | ✅ | ✅ |
| 6 | 트럭 | 5t | 인성2 | 신용 | ❌ | ❌ | ✅ | ✅ |

---

## 5. 구현 코드

### 5.1 분류 메서드 (ExternalAppController.cs)

```csharp
/// <summary>
/// 주문을 4개 외부앱별로 분류하여 각 큐에 적재
/// </summary>
/// <param name="order">분류할 주문</param>
/// <param name="stateFlag">주문 상태 플래그</param>
private void ClassifyAndEnqueueOrder(TbOrder order, PostgService_Common_OrderState stateFlag)
{
    // Step 1: 차량 타입 판단
    bool isMotorcycle = order.CarType == "오토";
    bool isSmallTruck = order.CarType == "트럭" &&
                        (order.CarWeight == "1t" || order.CarWeight == "1.4t");
    bool isLargeTruck = order.CarType == "트럭" &&
                        order.CarWeight != "1t" &&
                        order.CarWeight != "1.4t";

    Debug.WriteLine($"[분류 시작] KeyCode={order.KeyCode}, " +
                    $"CarType={order.CarType}, CarWeight={order.CarWeight}, " +
                    $"오토={isMotorcycle}, 소형트럭={isSmallTruck}, 대형트럭={isLargeTruck}");

    // Step 2-1: 오토바이 또는 1.4톤 이하 트럭 → 인성1, 인성2
    if (isMotorcycle || isSmallTruck)
    {
        // 인성1: 인성2 신용업체 제외
        if (!(order.CallCustFrom == StdConst_Network.INSUNG2 &&
              order.FeeType == "신용"))
        {
            QueueManager.EnqueueOrder(order, stateFlag, StdConst_Network.INSUNG1);
            Debug.WriteLine($"  → 인성1 큐 추가");
        }
        else
        {
            Debug.WriteLine($"  → 인성1 제외 (인성2 신용업체)");
        }

        // 인성2: 인성1 신용업체 제외
        if (!(order.CallCustFrom == StdConst_Network.INSUNG1 &&
              order.FeeType == "신용"))
        {
            QueueManager.EnqueueOrder(order, stateFlag, StdConst_Network.INSUNG2);
            Debug.WriteLine($"  → 인성2 큐 추가");
        }
        else
        {
            Debug.WriteLine($"  → 인성2 제외 (인성1 신용업체)");
        }
    }

    // Step 2-2: 1.4톤 이하 또는 초과 트럭 → 화물24시, 원콜
    if (isSmallTruck || isLargeTruck)
    {
        QueueManager.EnqueueOrder(order, stateFlag, StdConst_Network.CARGO24);
        Debug.WriteLine($"  → 화물24시 큐 추가");

        QueueManager.EnqueueOrder(order, stateFlag, StdConst_Network.ONECALL);
        Debug.WriteLine($"  → 원콜 큐 추가");
    }
}
```

### 5.2 호출 예시

```csharp
// 기존 주문 로드 시
public void LoadExistingOrders(List<TbOrder> orders)
{
    foreach (var order in orders)
    {
        // SeqNo로 StateFlag 결정
        var stateFlag = DetermineStateFlag(order);

        // 분류 및 큐 적재
        ClassifyAndEnqueueOrder(order, stateFlag);
    }
}

// 신규 주문 추가 시
public void AddNewOrder(TbOrder order)
{
    ClassifyAndEnqueueOrder(order, PostgService_Common_OrderState.Created);
}

// 주문 업데이트 시
public void UpdateOrder(TbOrder oldOrder, TbOrder newOrder)
{
    ClassifyAndEnqueueOrder(newOrder, PostgService_Common_OrderState.Updated_Assume);
}
```

---

## 6. 주의사항

### 6.1 절대 규칙
- ✅ `CarType == "오토"`는 **절대적 기준**
- ✅ `CarType == "트럭"`일 때만 CarWeight 체크
- ✅ 신용업체 필터링은 **인성1, 인성2에만 적용**
- ✅ 화물24시, 원콜은 **신용업체 무관** (모두 추가)

### 6.2 백업 로직과의 차이
**백업 로직** (잘못된 방식):
```csharp
// CarWeight만으로 판단 (CarType 미사용)
if (!string.IsNullOrEmpty(tb.CarWeight) &&
    tb.CarWeight != "1t" &&
    tb.CarWeight != "1.4t")
{
    // 화물
}
```

**새 로직** (올바른 방식):
```csharp
// CarType을 우선 체크, 트럭일 때만 CarWeight 체크
bool isMotorcycle = order.CarType == "오토";
bool isSmallTruck = order.CarType == "트럭" &&
                    (order.CarWeight == "1t" || order.CarWeight == "1.4t");
```

---

**작성 완료**: 2025-10-23
